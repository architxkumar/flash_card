name: flash_card
description: "A flash card app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  dynamic_color: ^1.8.1
  flutter_flip_card: ^0.0.6
  package_rename: ^1.10.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

